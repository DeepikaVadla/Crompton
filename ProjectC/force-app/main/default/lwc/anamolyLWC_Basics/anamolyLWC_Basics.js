import { LightningElement } from 'lwc';

export default class Hello extends LightningElement {
    //lightning web component use ECMA Script Modules that we import here using standard ECMA SCRIPT Syntax.
    //We are also using ECMA Script classes in this case "AnamolyLWC_Basics". This class can have properties
    greeting = 'World';
}